# CrewPathGuide 正式上线审计报告

审计日期：2026-09-15  
审计基线：`master` / `630354d Restructure job foundation courses`  
审计范围：路由、页面信息架构、12 个任务推进、学院课程、岗位包、求职中心、登录与权益、进度保存、AI/ASR 调用、数据库脚本、安全、运营与生产构建。

## 0. 实施进度更新

本报告提出的代码侧 P0 与 P1-1 至 P1-5 已于本轮完成：Retail 原子额度迁移、Task 6 岗位驱动、未知岗位 fail-closed、基础课进度上云、课程入口收敛、Task 7 口径统一、120 秒 ASR 上限和其他岗位 AI 制作中门槛均已落地。

同时完成了最小运营能力：AI 成功率/耗时/ASR 秒数/错误码日志、职业报告原子预留、管理员健康度面板、迁移顺序说明、只读生产核验、404、固定返回路由、页面 title/description，以及用户协议、隐私政策和岗位包交付与退款规则。

上线前仍需人工完成：

1. 按 `SUPABASE_MIGRATION_ORDER.md` 执行两份新迁移并运行只读核验。
2. 在 Vercel、Supabase 与百炼控制台开启预算或错误告警。
3. 用免费、Bar 付费、Retail 付费、管理员四种身份做真机全流程。
4. 由熟悉实际运营地区的专业人士复核协议和退款文字。

## 1. 执行结论

### 当前判断

- **Bar Server 付费封测：可以继续。** 主链路、注册邮件、Bar Server 基础课、场景训练、题库、模拟面试、激活码与 AI 原子额度均已有可用实现。
- **全站公开正式版：暂不建议直接宣布“全部功能正式可售”。** 目前有 2 个 P0、7 个 P1。它们主要集中在 Retail AI 权益、Task 6 岗位适配、跨设备课程进度、旧入口、任务完成口径、录音时长上限和生产监控。
- **最稳妥的上线口径：** 先把 Bar Server 标为“完整岗位包”，Retail 标为“课程预览/制作中”或修完 P0 后再售；其他岗位保留公开题库和课程制作中状态。

### 上线评分

| 领域 | 当前状态 | 结论 |
| --- | --- | --- |
| 生产构建 | 通过 | 可上线 |
| 12 任务主路径 | 基本完整 | 需统一 Task 7 完成口径与阶段命名 |
| Bar Server 基础课 | 完整 | 可封测 |
| Bar Server AI 场景与模拟 | 完整 | 可封测，需补录音硬上限 |
| Retail 基础课 | 已实现 | 可浏览，不宜收费开放 AI |
| Retail AI 场景与教练 | 数据库/提示词不完整 | P0 |
| 登录、邮件验证、激活码 | 已具备 | 需继续真机回归 |
| 权益与 AI 原子扣减 | Bar Server 完整 | Retail P0 |
| 跨设备训练记忆 | 部分完成 | 基础课仍主要依赖本地存储 |
| 支付 | 人工收款 + 激活码 | 适合封测，需补退款/交付规则 |
| 安全 | 基础加固已做 | 需生产库核验与异常监控 |
| 人工真机验收 | 未由本次自动审计覆盖 | 上线前必做 |

## 2. 已执行验证

### 自动检查

- `npm run lint`：通过。
- `npm run build`：通过，Vite 生产构建成功，2387 个模块完成打包。
- `npm run test:scenario`：通过，覆盖连续场景与重试契约。
- `npm run test:access`：通过，覆盖基础权益判断。
- `npm run test:career-report`：通过，覆盖职业报告服务契约。
- 静态资源引用检查：8 个显式 `/images`、`/audio`、`/assets` 引用均存在，未发现缺失文件。
- 未执行 `npm run test:qwen`：该脚本会调用真实模型，可能产生费用；不应在常规静态审计中反复运行。

### 生产页面抽查

- 首页可正常加载。
- `/programs/bar-server/foundation` 正确显示 9 天、Day 1 免费、Day 2-9 锁定。
- 匿名访问付费单日课程会出现登录/激活门槛。
- 错误的课程 day slug 会静默回到课程目录；错误的 job slug 可能进入错误边界，属于待修复的 P1。

## 3. 产品信息架构

### 建议固定的唯一心智模型

1. **登船路径**：告诉用户现在在哪一步、下一步是什么，只负责任务推进。
2. **海乘学院**：按岗位组织知识、短课、跟读、Guest Challenge、单题口语和岗位场景训练。
3. **求职中心**：简历、个人答案卡、完整 AI 模拟面试、申请渠道、申请记录、真实面试和 Offer。
4. **岗位包**：权益商品，不是第四套内容。它解锁学院与求职中心中属于该岗位的完整功能。

当前代码总体已经朝这个模型收敛，但仍有旧入口把“课程”带回大而全的 Task 5，造成用户感觉课程和路径是两套系统。

### 12 个任务的合理关系

| 任务 | 当前目标 | 主要区域 | 审计结论 |
| --- | --- | --- | --- |
| 1 | 海乘适配评估 | 首页/测评 | 清楚 |
| 2 | 选择目标岗位 | 登船路径 | 清楚，但应把已选岗位置顶到学院和求职中心 |
| 3 | 确定申请路线 | 登船路径 | 清楚 |
| 4 | 制作英文简历 | 求职中心 | 清楚；“返回路线”不应使用浏览器历史 |
| 5 | 完成目标岗位基础训练 | 学院 | 方向正确；Task 5 应只做导航和验收，不再承载重复长课 |
| 6 | 准备个人面试答案 | 求职中心 | Bar 可用；Retail AI 写死 Bar，P0 |
| 7 | 岗位单题口语 + AI 模拟 | 学院 + 求职中心 | 子步骤存在，但完成判定不一致，P1 |
| 8 | 真实面试跟进 | 求职中心 | 清楚 |
| 9 | 核对 Offer | 求职中心/我的 | 清楚 |
| 10 | 登船证件 | 登船路径 | 清楚 |
| 11 | 签证与通行许可 | 登船路径 | 清楚 |
| 12 | 出发准备 | 登船路径 | 清楚 |

### 阶段命名问题

当前第三阶段叫“拿到 Offer”，但其中包含 Task 7 训练和 Task 8 真实面试，用户此时还没有 Offer。建议改为“面试与 Offer”，避免进度语义倒置。

## 4. 必须先修的 P0

### P0-1：Retail AI 在生产数据库会被额度函数拒绝

**证据**

- 服务端已经识别 `retail_sales_pack`，并会为 Retail 转写、场景追问、评分和模拟面试调用额度 RPC。
- `supabase_ai_cost_controls.sql`、`supabase_ai_usage_reservations.sql`、`supabase_launch_hardening.sql` 中的 `reserve_ai_usage_quota` 都仍明确拒绝除 `bar_server_pack` 以外的产品代码。
- `supabase_retail_sales_pack.sql` 只创建了产品与权益数据，没有升级额度 RPC。

**用户后果**

Retail 用户可以看到课程和场景入口，但真正点击 AI 转写或评分时会收到额度请求无效/训练不可用。这属于收费后核心功能不可用。

**修复方案**

1. 新增一份只向前执行的迁移，不修改或重跑旧基线 SQL。
2. 允许 `bar_server_pack` 与 `retail_sales_pack`。
3. 免费场景继续只允许 Bar Server 的 3 个指定场景。
4. 增加 Retail 的 reserve/finalize/record 自动测试。
5. 用一个 Retail 测试权益账号在生产环境跑：转写、追问、评分、完整模拟、失败释放额度。

### P0-2：Task 6 AI 答案教练写死 Bar Server

**证据**

- Task 6 的 AI 教练只检查 `bar_server_pack`。
- 请求参数固定为 `position: 'Bar Server'`。
- 服务端答案教练系统提示词固定为 Bar Server 招聘和酒吧服务。
- Retail 用户进入同一个 Task 6 时，要么被要求购买 Bar 包，要么得到错误岗位的建议。

**修复方案**

1. 从目标岗位派生 `position`、`productCode` 和岗位提示词。
2. 第一版只给 Bar 与 Retail 开 AI 教练；其他岗位显示“AI 教练制作中”，保留纯文本答案卡。
3. 答案卡的岗位知识卡按岗位切换：Bar 使用酒水/责任售酒，Retail 使用需求发现/产品知识/KPI/防损。
4. Task 6 完成后，Bar 与 Retail 都跳到自己的 Task 7 单题训练入口。

## 5. 正式公开前应修的 P1

### P1-1：基础课进度、收藏与分班结果没有完整上云

独立基础课页面直接写 `localStorage`：

- Bar 进度在 `task5_data.foundationProgress`。
- Retail 进度在独立本地 key。
- 收藏表达与分班结果也是本地 key。

只有用户回到旧 Task 5 并完成任务时，部分数据才有机会进入 `job_preparation_profile`。换手机、清浏览器、无痕模式或 Safari WebView 都可能丢失已付费学习记录。

**修复方案**

- 登录用户每完成一个小步骤就防抖 upsert 到云端，而不是等整个 Task 5 完成。
- 打开课程先合并云端与本地，以最新 `updatedAt` 为准。
- 保存 `courseVersion`，课程更新后不能把旧记录误判为新课程已完成。
- 收藏表达和 placement 至少按岗位写入同一 profile JSON；以后再拆表。

### P1-2：仍有旧 Task 5 入口绕路

需要统一修正的入口：

- 公开题库页的“岗位基础课”。
- Bar 免费体验完成页的“岗位基础课”。
- Task 7 知识训练结果页的“查看任务5能力状态”。
- 首页“继续基础课”的推荐动作。
- Bar 岗位包中“从任务5基础课程开始”的旧文案。

**目标路由**

- Bar：`/programs/bar-server/foundation`
- Retail：`/programs/retail/foundation`
- 未制作岗位：学院岗位页显示“课程制作中”，不要跳到通用 Task 5 长页。

### P1-3：Task 7 标题和完成判定不一致

路径标题写“完成岗位口语与 AI 模拟”，但任务列表只依据 `progress.task7` 判断完成；`task7AiMock` 是另一条记录。因此用户只完成一组单题语音，也可能看到 Task 7 已完成。

**建议选择一个明确口径**

- 推荐：Task 7 页面显示两个子步骤；“单题岗位口语”完成后记子进度，“完整 AI 模拟”完成后任务才完成。
- 若免费用户无需完整模拟，则把主标题改为“完成岗位口语训练”，AI 模拟作为岗位包增强项，不参与 12 任务硬阻塞。

不要继续维持“文案要求两个、代码只检查一个”的状态。

### P1-4：录音次数有限，但单次时长没有全链路硬限制

- 免费 Bar 场景自动停止 120 秒。
- 付费岗位场景自动停止 90 秒。
- Task 7 与完整模拟面试没有看到同等的自动停止逻辑。
- 客户端限制音频文件 2.5MB，服务端限制 data URL 3.5M 字符，但没有验证真实音频秒数。
- 32kbps 下 2.5MB 理论上可接近 625 秒，远高于页面期望的 30-120 秒。

**修复方案**

- 所有录音组件共用一个 hook，统一 90 秒或 120 秒自动停止。
- 客户端上传 `durationSeconds`，服务端拒绝超过上限的请求；不要只相信前端值，最好读取音频元数据或按保守字节/时长双重限制。
- 转写成功事件保存实际秒数和估算成本，后台能按日、用户、产品看总秒数。
- 失败重录不扣成功额度，但设置现有 24 小时失败上限继续防刷。

### P1-5：其他岗位的完整模拟权益边界不清楚

服务端除 Retail 外的所有岗位都默认映射到 Bar Server 产品。Task 8 前端对 Bar/Retail 指定产品，其他岗位传空值并可能回落到旧全站权限。

**修复方案**

- v1 只开放 Bar 与 Retail AI 模拟。
- 其他岗位可浏览真实题库和文字准备，但按钮显示“完整 AI 模拟制作中”。
- 未来新增岗位时通过岗位目录映射产品，禁止“未知岗位默认使用 Bar 权益”。

### P1-6：生产错误缺少可追踪监控

当前 ErrorBoundary 与 API 错误主要写 `console.error`；管理员反馈页能看用户反馈，但不能自动看到：

- 哪个 API 失败最多。
- 百炼返回码和模型延迟。
- Supabase RPC 失败率。
- 邮件验证回调失败原因。
- 某次额度已预留但结果未返回。

**修复方案**

- 最小版先建立服务端结构化错误表/日志：requestId、userId hash、route、action、model、status、latency、errorCode，不保存原始录音和完整敏感回答。
- Vercel 设置预算和函数错误告警；阿里云百炼设置费用告警。
- 管理员页增加最近 24 小时 AI 成功率、ASR 秒数、失败码 Top 5。

### P1-7：数据库脚本存在执行顺序漂移风险

仓库中存在多份会重建同名额度函数的 SQL。旧脚本如果以后被重新执行，可能覆盖较新的原子预留逻辑或重新只允许 Bar Server。

**修复方案**

- 改用不可变、带时间戳的迁移目录。
- 旧 SQL 标记为 baseline/已执行，禁止重复运行。
- 新增只读生产核验脚本，检查表、函数签名、函数定义摘要、RLS、授权和产品配置，不做任何写入。
- 每次部署先跑 schema verification，再跑前端冒烟测试。

## 6. 可排在上线后的 P2

1. **未知路由与错误课程 slug**：当前通配路由直接回首页，错误 day slug 静默回目录。应提供 404/课程不存在页面，并记录来源 URL。
2. **底部导航高亮**：只做完全路径匹配；进入 `/programs/*`、`/academy/*`、`/jobs/*` 子页时所属栏目不一定高亮。
3. **返回按钮**：Task 4 与服务说明使用 `navigate(-1)`；从外部链接直达时可能返回站外。主流程按钮应回固定安全路由。
4. **Task 2/3 双重完成交互**：页面内保存按钮与通用任务 footer 并存，建议统一成一个主要动作。
5. **组件命名债务**：`Task8MockInterview` 实际挂在 Task 7 mock 路由，容易让后续维护误接 Task 8。
6. **页面标题与 SEO**：多数路由共用同一 document title；公开学院页、岗位包页、测评页应有独立标题和描述。
7. **可访问性**：部分图标按钮缺少可读名称，弹窗关闭按钮在无障碍树中可能只显示“button”。
8. **课程长页性能**：Task 5 仍是较大的旧聚合页。完成导航收敛后可拆除重复课程正文，降低认知和维护成本。

## 7. AI 与语音成本审计

价格基准来自阿里云百炼官方模型价格页，审计日价格：

- `qwen-audio-3.0-asr-flash` 北京区：**0.00022 元/秒**，输出文本不另收费。
- `qwen3.5-plus` 在 128K 以下：输入 0.8 元/百万 Token，非思考输出 4.8 元/百万 Token。
- `qwen3.7-plus` 在 256K 以下：输入 2 元/百万 Token，非思考输出 8 元/百万 Token。
- 价格与活动可能调整，上线预算应以百炼控制台账单为最终依据。

官方来源：https://help.aliyun.com/zh/model-studio/model-pricing

### 哪些语音功能不产生你的 AI 账单

| 功能 | 技术 | 费用 |
| --- | --- | --- |
| 题目朗读 | 浏览器 `speechSynthesis` | 不走百炼 |
| Task 5 跟读录音与回放 | 浏览器 MediaRecorder，本地回放 | 不走百炼 |
| Task 5 Guest Challenge | 浏览器 SpeechRecognition/MediaRecorder | 不走服务端 AI；浏览器能力不可用时退回文字 |
| Task 6 填写答案卡的语音输入 | 浏览器 SpeechRecognition | 不走百炼 |

浏览器自然语音的质量取决于设备、浏览器和已安装/在线语音，网站不能强制所有设备都使用 Edge 的某个 Natural Voice。

### 会产生费用的功能

| 功能 | 一次完整动作 | 成功额度消耗 |
| --- | --- | --- |
| Bar 免费场景 | 每次尝试 1 次 ASR + 1 次 AI 评价 | 每场景最多 2 次尝试 |
| 付费岗位场景 | 2 次 ASR + 1 次动态追问 + 1 次最终评分 | 2 次转写 + 2 次 AI 反馈 |
| Task 6 AI 答案教练 | 一次追问/重写 | 1 次 AI 反馈 |
| Task 7 单题口语报告 | 每道已录题 1 次 ASR，整组 1 次报告 | N 次转写 + 1 次 AI 反馈 |
| 完整 AI 模拟面试 | 每题 1 次 ASR，整场 1 次报告 | N 次转写 + 1 次模拟面试 |
| AI 职业报告 | 每账户最多一份 | 独立接口，不计岗位包额度 |

### 免费用户理论上限

免费 Bar 体验为 3 个场景，每场最多 2 次尝试：

- 最多 6 次 ASR。
- 最多 6 次文本评价。
- 若每次 60 秒，ASR 约 **0.0792 元/人**。
- 若每次都到 120 秒，ASR 约 **0.1584 元/人**。
- 文本评价通常是分币级；实际总成本取决于输出长度。

另外每个已验证账户可生成 1 份职业报告。现有“先 count 再生成”不是严格原子操作，恶意并发请求理论上可能重复通过，建议改为数据库唯一预留或幂等请求。

### 付费岗位包理论上限

标准配置：360 次 ASR、120 次 AI 反馈、10 次完整模拟面试。

- 360 次录音每次 60 秒：ASR 约 **4.752 元/人**。
- 360 次录音每次 120 秒：ASR 约 **9.504 元/人**。
- 当前极端技术上限：32kbps、2.5MB 文件可能接近 625 秒；若恶意用户把 360 次都录满，ASR 理论值约 **49.5 元/人**。这就是必须补服务端秒数硬限制的原因。
- 120 次普通反馈即使每次都输出 2500 Token，按 qwen3.5-plus 输出价约 **1.44 元**，另加少量输入费用。
- 若 120 次都使用 qwen3.7-plus 场景评分且每次输出 1800 Token，输出约 **1.728 元**，另加少量输入费用。
- 10 次完整模拟每次达到 6000 输出 Token，按 qwen3.5-plus 输出约 **0.288 元**，另加输入费用。

因此正常用户的岗位包 AI 成本大致可控制在十元以内；当前最坏情况主要由超长 ASR 拉高，修复 90-120 秒硬上限后，`¥199 / 180 天` 的毛利空间是健康的。

### 原子额度实现评价

已具备：

- 数据库预留额度。
- 成功/失败 finalize。
- requestId 记录与事件去重。
- 失败不占成功配额。
- 24 小时失败次数上限。
- 文本输入作为 ASR 用尽后的免费替代路径。

仍需补：

- Retail 产品代码支持。
- 保存真实 ASR 秒数、输入/输出 Token 与估算金额。
- Task 7/完整模拟统一自动停止。
- 普通转写重试的 requestId 幂等恢复；网络断开但模型已成功时，用户目前可能丢结果并再次消耗一次调用。
- 职业报告原子“一账户一次”。

## 8. 登录、权益、支付与数据

### 已经具备

- Supabase 邮箱验证回调页面。
- Resend 自定义 SMTP 已经实际投递到 QQ 邮箱。
- 登录、激活码、管理员与岗位权益分离。
- 人工付款请求、支持请求、管理员 Beta 数据页。
- Bar Founder 用户权益迁移。
- 文章读取前有 DOMPurify 清洗；数据库脚本也撤销了普通用户对文章的写权限。
- 申请记录、真实面试、Offer、路径档案和部分训练记录上云。

### 仍需正式验收

- 邮件客户端内置浏览器、Safari、微信 WebView、QQ 邮箱 App 的验证链接。
- 过期链接、重复点击、已验证账户再次登录、换设备登录。
- 免费账户不可访问 Day 2、付费场景、完整模拟；管理员可以。
- 激活码重复使用、错误产品码、过期权益、退出登录后缓存刷新。
- 人工收款页应明确：商品、价格、有效期、额度、交付时间、退款规则、联系方式和异常处理。

### 法务与用户信任

已有“服务与数据说明”，但它还不是完整的用户协议、隐私政策和购买/退款规则。正式公开收款前建议至少补三个独立可访问文本，并在注册和付款确认处显式勾选。具体合规内容应由熟悉目标运营地区的专业人士复核，本报告不替代法律意见。

## 9. 推荐的一次性收尾顺序

### 第 1 批：阻断收费故障

1. 修 Retail 原子额度迁移与自动测试。
2. Task 6 改成岗位驱动，Bar/Retail 各用正确权益和提示词。
3. 未支持岗位禁止默认落到 Bar 产品。

### 第 2 批：收敛路径

1. 修 5 处旧 Task 5 入口。
2. 明确 Task 7 完成口径。
3. 改第三阶段为“面试与 Offer”。
4. 修固定返回路由、底部导航归属、404 与错误课程 slug。

### 第 3 批：保护付费学习资产和成本

1. 基础课逐步进度、收藏、placement 上云并支持跨设备合并。
2. 抽取统一录音 hook，所有付费 ASR 设置 90-120 秒硬上限。
3. AI 事件记录秒数、Token、延迟、错误码和估算成本。
4. 职业报告增加原子一次性预留。

### 第 4 批：运营与信任

1. 完整隐私政策、用户协议、退款/交付规则。
2. Vercel、Supabase、百炼费用和错误告警。
3. 管理后台增加 AI 成功率和成本视图。
4. 课程与公开页面独立 title/description。

### 第 5 批：三身份真机验收

按免费用户、Bar 付费用户、Retail 付费用户、管理员四种身份跑完整清单。每条都记录设备、浏览器、账号、时间、结果、截图和 requestId。

## 10. 最终上线门槛

只有同时满足下面条件，才建议宣布正式版 1.0：

- P0 全部关闭。
- P1-1 至 P1-5 关闭，P1-6 至 P1-7 至少有可用的最小实现。
- 生产库 schema verification 全绿。
- 四身份真机全流程通过。
- 百炼成本告警已启用，并观察至少 3-7 天真实封测数据。
- 注册验证失败率、AI 成功率、平均录音秒数、每用户成本、激活成功率均能从后台得到。
- 商品页、服务说明、隐私/协议/退款文字一致，没有旧价格或旧微信号。

## 11. 本次审计的边界

- 本次能够确认代码仓库、生产构建、自动测试和抽查页面行为。
- 无法仅凭仓库证明 Supabase 生产库里每一份 SQL 都按正确顺序执行；必须用只读 schema verification 查询确认。
- 无法替代用户负责的真实手机、QQ 邮箱、微信 WebView、弱网和麦克风权限测试。
- 百炼价格来自审计日官方页面，实际账单与优惠以控制台为准。
